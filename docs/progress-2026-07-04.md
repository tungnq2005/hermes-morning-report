# Nhật ký tiến độ — 2026-07-04 (sau D2)

Tóm tắt những việc hoàn thành trong ngày, tiếp nối sau khi D2 (Document Conversion) đã chạy được.

## 1. Chốt hướng D3 + làm rõ hạ tầng
- Duyệt kế hoạch D3 (Technical Setup / bàn giao).
- Làm rõ nhầm lẫn cũ: **VPS + cài NATIVE (không Docker)** — Docker mới là thứ làm mất tool `browser`, không phải VPS. WSL hiện tại là dev.

## 2. Nâng cấp bộ script triển khai VPS (`morning-brief-setup/`)
- `config.env` mới: tham số hoá `OC_USER`, port, timezone, giờ gửi, search provider — **bỏ hết hardcode `ubuntu`**.
- `01_system_prep.sh`: thêm deps cho D2 (LibreOffice, python-docx/pptx/pypdf) và Google API libs.
- `06_bootstrap_skill.sh`: cài **cả 2 skill** (morning-report + doc-convert đóng gói local) + áp bản vá `audio-runtime.md`.
- `05_migrate_secrets.sh`: chuyển sang non-interactive (audit sạch).
- Thêm `07_configure_integrations.sh` (tool profile, search provider, model, command owner), `08_searxng.sh` (tùy chọn), `healthcheck.sh` (bằng chứng ổn định 48h).

## 3. Tài liệu bàn giao song ngữ (Việt–Anh)
- `user-guide`, `operator-runbook`, `chat-commands`, `handover-session` (kịch bản 30 phút + checklist nghiệm thu).

## 4. Tích hợp Google Workspace cho D2 (làm mới hôm nay)
- OAuth client (Desktop) đã kết nối; authorize thành công (`token.json`).
- Module `google_io.py` + `authorize_google.py`.
- convert.py thêm 2 chế độ **direct-to-cloud** `--to gdoc` / `--to gslides`, và **tự đọc file Google riêng tư**.
- **Test thật OK**: tạo Google Doc + Google Slides mới, và đọc ngược file riêng tư → PDF.
- Bảo mật: creds quyền `600`, không lọt vào bundle.

## 5. Sửa lỗi cron "LLM request failed"
- Nguyên nhân: run chạy >9 phút bị watchdog hủy (model reasoning `pro` compose lâu + search 429 kéo dài) — **không phải** lỗi key/balance.
- Sửa: default model → `deepseek-v4-flash`, fallback → `deepseek-v4-pro`.
- **Verify**: ép chạy lại xong trong ~140s, gửi đủ text + audio.

## Trạng thái & việc còn lại
- ✅ D1, D2 chạy được; Google Workspace xong; bộ deploy + tài liệu sẵn sàng; 14/14 unit test D2 pass.
- ⏳ **Chốt search provider** thay Brave (đang 429) — Tavily / SearXNG / Brave-paid.
- ⏳ **Deploy VPS + soak test 48h** (WS3) — chạy khi có VPS.
- ⚠️ Rotate lại Google client secret sau bàn giao (đã dán qua chat lúc setup).
