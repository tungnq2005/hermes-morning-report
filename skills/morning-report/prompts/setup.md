# Morning Report Setup

Use for first-time setup only.

Run the command below, then follow the returned `next_action`.

## Start

Run:

```bash
python3 skills/morning-report/scripts/setup/run.py system --agent --compact --full-readiness
```
